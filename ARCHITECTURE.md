# 📊 Authentication System Architecture

## System Overview

```
┌─────────────────────────────────────────────────────────────┐
│                    Quiz Practice App                         │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │   Signup     │  │    Login     │  │   Practice   │      │
│  │  /auth/      │  │   /auth/     │  │   /practice  │      │
│  │  signup      │  │   login      │  │              │      │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘      │
│         │                 │                 │               │
│         └─────────────────┼─────────────────┘               │
│                           │                                 │
│                    ┌──────▼───────┐                        │
│                    │   Header      │ (Auth Navigation)    │
│                    │   Component   │                        │
│                    └──────┬───────┘                        │
│                           │                                 │
└───────────────────────────┼─────────────────────────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
        ▼                   ▼                   ▼
   ┌─────────┐         ┌─────────┐       ┌──────────┐
   │Middleware│        │Server   │       │Database  │
   │Protection│        │Actions  │       │(PostgreSQL│
   └─────────┘        │         │       │ Prisma)  │
   (middleware.ts)    │- signup │      │          │
                      │- verify │      │User Model│
   - Admin Check      │- login  │      └──────────┘
   - User Auth       │- logout │
   - Redirect        └─────────┘
                           │
              ┌────────────┼────────────┐
              │            │            │
              ▼            ▼            ▼
          ┌────────┐  ┌────────┐  ┌────────┐
          │ Hashing│  │ Cookies│  │ Email  │
          │(PBKDF2)│  │ Mgmt   │  │Service │
          └────────┘  └────────┘  └────────┘
          (auth.ts)   (auth.ts)   (email.ts)
```

## Authentication Flow Diagrams

### 1. Signup & Email Verification Flow

```
┌─────────────────────────────────────────────────────────────┐
│ SIGNUP & EMAIL VERIFICATION                                │
├─────────────────────────────────────────────────────────────┤

START
  │
  ▼
┌────────────────────────────────────────┐
│ User visits /auth/signup               │
│ Sees: SignupForm                       │
│ Inputs: name, email, password, confirm│
└────────────────────┬───────────────────┘
                     │
                     ▼
            ┌────────────────────┐
            │ Form Validation    │
            │ Client-side        │
            └────────┬───────────┘
                     │
                     ▼
        ┌────────────────────────────┐
        │ POST to signupAction()      │
        │ (Server Action)            │
        └────────┬───────────────────┘
                 │
                 ▼
    ┌────────────────────────────────┐
    │ Server Validation              │
    │ - Check required fields        │
    │ - Validate email format        │
    │ - Check password length (6+)   │
    │ - Match password & confirm     │
    └────────┬───────────────────────┘
             │
             ▼
    ┌────────────────────────────────┐
    │ Check Email Uniqueness         │
    │ Query: User.findUnique({email})│
    └────────┬───────────────────────┘
             │
             ├─ Email exists?
             │  └─ Return error ❌
             │
             └─ Email unique? ✓
                │
                ▼
    ┌────────────────────────────────┐
    │ Hash Password                  │
    │ PBKDF2-SHA512                  │
    │ Iterations: 1000               │
    │ Output: hex string             │
    └────────┬───────────────────────┘
             │
             ▼
    ┌────────────────────────────────┐
    │ Generate Verification Code     │
    │ 6 uppercase alphanumeric chars │
    │ Example: ABC123                │
    └────────┬───────────────────────┘
             │
             ▼
    ┌────────────────────────────────┐
    │ Set Code Expiration            │
    │ 15 minutes from now            │
    │ Store in DB                    │
    └────────┬───────────────────────┘
             │
             ▼
    ┌────────────────────────────────┐
    │ Create User in Database        │
    │ INSERT User                    │
    │ Fields: name, email, password  │
    │         code, expiresAt        │
    │         emailVerified: false   │
    └────────┬───────────────────────┘
             │
             ▼
    ┌────────────────────────────────┐
    │ Send Verification Email        │
    │ (or log code in dev)           │
    │                                │
    │ Development mode:              │
    │ → Log to console               │
    │                                │
    │ Production mode:               │
    │ → Call email service API       │
    │   (Resend, SendGrid, etc.)     │
    └────────┬───────────────────────┘
             │
             ▼
    ┌────────────────────────────────┐
    │ Redirect User                  │
    │ to /auth/verify-email?email=.. │
    │                                │
    │ User sees: VerifyEmailForm     │
    │ Input: 6-digit code            │
    └────────┬───────────────────────┘
             │
             ▼
    ┌────────────────────────────────┐
    │ POST Code to verifyEmailAction()│
    │ (Server Action)                │
    └────────┬───────────────────────┘
             │
             ▼
    ┌────────────────────────────────┐
    │ Validate Code                  │
    │ - Find user by email           │
    │ - Compare code match           │
    │ - Check code expiration        │
    │ - Check email not verified yet │
    └────────┬───────────────────────┘
             │
             ├─ Code invalid?
             │  └─ Return error ❌
             │
             ├─ Code expired?
             │  └─ Return error ❌
             │
             ├─ Email already verified?
             │  └─ Return error ❌
             │
             └─ All checks pass ✓
                │
                ▼
    ┌────────────────────────────────┐
    │ Update User in Database        │
    │ emailVerified = true           │
    │ verificationCode = null        │
    │ verificationCodeExpiresAt=null │
    └────────┬───────────────────────┘
             │
             ▼
    ┌────────────────────────────────┐
    │ Set Session Cookie             │
    │ USER_COOKIE = {userId, email}  │
    │ HttpOnly, SameSite, 30 days    │
    └────────┬───────────────────────┘
             │
             ▼
    ┌────────────────────────────────┐
    │ Redirect to /auth/login        │
    │                                │
    │ User now ready to login        │
    └────────────────────────────────┘

END ✓
```

### 2. Login Flow

```
┌─────────────────────────────────────────────────────────────┐
│ LOGIN                                                       │
├─────────────────────────────────────────────────────────────┤

START
  │
  ▼
┌────────────────────────────────────────┐
│ User visits /auth/login                │
│ Sees: LoginForm                        │
│ Inputs: email, password                │
└────────────────────┬───────────────────┘
                     │
                     ▼
            ┌────────────────────┐
            │ Form Validation    │
            │ Client-side        │
            └────────┬───────────┘
                     │
                     ▼
        ┌────────────────────────────┐
        │ POST to loginAction()       │
        │ (Server Action)            │
        └────────┬───────────────────┘
                 │
                 ▼
    ┌────────────────────────────────┐
    │ Server Validation              │
    │ - Check required fields        │
    │ - Validate email format        │
    └────────┬───────────────────────┘
             │
             ▼
    ┌────────────────────────────────┐
    │ Find User by Email             │
    │ Query: User.findUnique({email})│
    └────────┬───────────────────────┘
             │
             ├─ User not found?
             │  └─ Return error ❌
             │     "Invalid email or password"
             │
             └─ User found ✓
                │
                ▼
    ┌────────────────────────────────┐
    │ Check Email Verified           │
    │ user.emailVerified === true?   │
    └────────┬───────────────────────┘
             │
             ├─ Email not verified?
             │  └─ Return error ❌
             │     "Please verify your email first"
             │
             └─ Email verified ✓
                │
                ▼
    ┌────────────────────────────────┐
    │ Verify Password                │
    │ - Hash submitted password      │
    │ - Compare with stored hash     │
    │ - Constant-time comparison     │
    └────────┬───────────────────────┘
             │
             ├─ Password wrong?
             │  └─ Return error ❌
             │     "Invalid email or password"
             │
             └─ Password correct ✓
                │
                ▼
    ┌────────────────────────────────┐
    │ Set Session Cookie             │
    │ USER_COOKIE = {                │
    │   userId: string,              │
    │   email: string                │
    │ }                              │
    │                                │
    │ Options:                       │
    │ - HttpOnly: true               │
    │ - SameSite: lax                │
    │ - Secure: true (prod only)     │
    │ - Path: /                      │
    │ - MaxAge: 30 days              │
    └────────┬───────────────────────┘
             │
             ▼
    ┌────────────────────────────────┐
    │ Redirect User                  │
    │ to /practice                   │
    │                                │
    │ User is now authenticated      │
    │ Can access protected routes    │
    └────────────────────────────────┘

END ✓
```

### 3. Route Protection with Middleware

```
┌─────────────────────────────────────────────────────────────┐
│ MIDDLEWARE PROTECTION                                       │
├─────────────────────────────────────────────────────────────┤

Request incoming to /admin/* or /practice/*
  │
  ▼
┌───────────────────────────────────────┐
│ middleware.ts checks request path     │
└───────────────────────────────────────┘
  │
  ├─ Path: /admin/* and not /admin/login?
  │  │
  │  ▼
  │  Check mcq_admin cookie
  │  ├─ Cookie = "1"? ✓ → Allow access
  │  └─ No cookie?   → Redirect to /admin/login
  │
  ├─ Path: /practice (home page)?
  │  │
  │  ▼
  │  Check mcq_user cookie
  │  ├─ Cookie exists? ✓ → Allow access
  │  └─ No cookie?   → Redirect to /auth/login?next=/practice
  │
  └─ Other paths?
     → Allow access (no auth required)
```

### 4. Cookie & Session Management

```
┌─────────────────────────────────────────────────────────────┐
│ SESSION MANAGEMENT                                          │
├─────────────────────────────────────────────────────────────┤

After successful login/verification:

┌──────────────────────────────────────┐
│ setUserCookie(userId, email)         │
└──────────────────────────────────────┘
  │
  ▼
┌──────────────────────────────────────┐
│ Browser Cookie Storage               │
├──────────────────────────────────────┤
│ Name: mcq_user                       │
│ Value: {"userId":"abc","email":"..."}│
│                                      │
│ Attributes:                          │
│ - HttpOnly: true (JS cannot access) │
│ - SameSite: lax (CSRF protection)   │
│ - Secure: true (HTTPS only in prod) │
│ - Path: / (all routes)              │
│ - MaxAge: 2592000 (30 days)         │
└──────────────────────────────────────┘

Every request to /practice or /admin:
  │
  ▼
┌──────────────────────────────────────┐
│ Middleware checks cookies            │
│ - Reads mcq_user or mcq_admin       │
│ - Validates presence                │
│ - Allows or redirects               │
└──────────────────────────────────────┘

On logout:
  │
  ▼
┌──────────────────────────────────────┐
│ clearUserCookie()                    │
│ Sets cookie MaxAge: 0 (delete)      │
└──────────────────────────────────────┘
```

## Database Schema Diagram

```
┌───────────────────────────────────────────────────────────┐
│ User Table (PostgreSQL)                                   │
├───────────────────────────────────────────────────────────┤
│                                                           │
│ id: String (CUID) ─────────────────────────── PRIMARY KEY│
│                                                           │
│ name: String ────────────────────── User's display name  │
│                                                           │
│ email: String (UNIQUE) ──────────── Email address        │
│                         └─ Prevents duplicates            │
│                                                           │
│ password: String ────────────────── PBKDF2 hashed        │
│           └─ Never plaintext                             │
│           └─ 1000 iterations of SHA-512                  │
│                                                           │
│ emailVerified: Boolean ──────────── Verified flag        │
│                └─ false by default                       │
│                └─ true after email verification          │
│                                                           │
│ verificationCode: String? ───────── Temporary 6-char code│
│                  └─ null when verified                   │
│                  └─ Regenerated on each signup           │
│                                                           │
│ verificationCodeExpiresAt: DateTime? ──── Code TTL      │
│                          └─ 15 minutes                   │
│                          └─ null when verified           │
│                                                           │
│ createdAt: DateTime ───────────────── Account creation   │
│ updatedAt: DateTime ───────────────── Last modified      │
│                                                           │
└───────────────────────────────────────────────────────────┘
```

## File Organization

```
src/
│
├── lib/ (Authentication Logic)
│   ├── auth.ts
│   │   ├── Session management
│   │   ├── Password hashing
│   │   └── Verification codes
│   │
│   └── email.ts
│       └── Email service integration
│
├── app/ (Pages & Routes)
│   │
│   ├── auth/ (Authentication routes)
│   │   │
│   │   ├── signup/
│   │   │   ├── page.tsx (Form page)
│   │   │   ├── SignupForm.tsx (React component)
│   │   │   ├── VerifyEmailForm.tsx (Verification component)
│   │   │   └── actions.ts (Server functions)
│   │   │
│   │   ├── login/
│   │   │   ├── page.tsx (Form page)
│   │   │   ├── LoginForm.tsx (React component)
│   │   │   └── actions.ts (Server functions)
│   │   │
│   │   └── verify-email/
│   │       └── page.tsx (Verification page)
│   │
│   ├── practice/ (Protected route)
│   │   └── [quizId]/ (Public quiz pages)
│   │
│   └── admin/ (Admin route)
│       └── login/ (Password-based login)
│
├── components/
│   ├── AuthNav.tsx (Login/signup buttons or user menu)
│   └── Header.tsx (Updated with AuthNav)
│
├── middleware.ts (Route protection)
│
└── prisma/
    ├── schema.prisma (User model)
    └── migrations/ (Database versions)
```

## Security Architecture

```
┌──────────────────────────────────────────────────────┐
│ CLIENT (Browser)                                     │
├──────────────────────────────────────────────────────┤
│ Forms submit via HTTPS                              │
│ Cookies sent automatically (HttpOnly)               │
│ No sensitive data in localStorage                   │
└────────────┬─────────────────────────────────────────┘
             │ HTTPS Only
             ▼
┌──────────────────────────────────────────────────────┐
│ SERVER (Next.js)                                    │
├──────────────────────────────────────────────────────┤
│ 1. Input Validation                                 │
│    - Required fields                                │
│    - Email format                                   │
│    - Password length                                │
│                                                     │
│ 2. Database Checks                                  │
│    - Email uniqueness                              │
│    - User existence                                │
│    - Email verification status                     │
│                                                     │
│ 3. Password Security                               │
│    - Hash with PBKDF2 (1000 iterations)           │
│    - Constant-time comparison                      │
│    - Never log or display                          │
│                                                     │
│ 4. Session Management                              │
│    - HttpOnly cookies (JS cannot access)          │
│    - SameSite=Lax (CSRF protection)               │
│    - Secure flag in production                     │
│    - 30-day expiration                            │
│                                                     │
│ 5. Middleware Protection                           │
│    - Check auth status before route access        │
│    - Redirect unauthorized requests               │
│    - Verify cookie signature                      │
└────────────┬─────────────────────────────────────────┘
             │
             ▼
┌──────────────────────────────────────────────────────┐
│ DATABASE (PostgreSQL)                               │
├──────────────────────────────────────────────────────┤
│ - Passwords stored as hashes only                  │
│ - Email field has UNIQUE constraint                │
│ - Temporary codes with TTL                        │
│ - Encrypted connection (optional)                  │
└──────────────────────────────────────────────────────┘
```

## Deployment Architecture

```
┌─────────────────────────────────────────────┐
│ Development                                 │
├─────────────────────────────────────────────┤
│ npm run dev                                 │
│ localhost:3000                              │
│                                             │
│ Features:                                   │
│ - Codes logged to console                  │
│ - No email service needed                  │
│ - Hot reload                               │
│ - Debug mode active                        │
└─────────────────────────────────────────────┘

                    ▼

┌─────────────────────────────────────────────┐
│ Production (Vercel/Any Platform)            │
├─────────────────────────────────────────────┤
│ npm run build && npm start                  │
│ https://your-domain.com                     │
│                                             │
│ Configuration:                              │
│ ✓ DATABASE_URL = production DB             │
│ ✓ EMAIL_SERVICE_URL = Resend/SendGrid      │
│ ✓ EMAIL_API_KEY = valid API key            │
│ ✓ NODE_ENV = production                    │
│ ✓ ADMIN_PASSWORD = strong password         │
│ ✓ HTTPS enabled                            │
│                                             │
│ Security Features:                         │
│ ✓ Cookies marked Secure                    │
│ ✓ HTTPS enforced                           │
│ ✓ Email verification working               │
│ ✓ Password hashing enabled                 │
│ ✓ Rate limiting configured                 │
│ ✓ Error logging enabled                    │
└─────────────────────────────────────────────┘
```

---

This visual documentation helps understand:
- How data flows through the system
- Where security measures are applied
- Which components handle which responsibilities
- How to trace requests through the stack
